import { JSONData } from "./interface";

export type DataCollectionElement =
  | HTMLInputElement
  | HTMLSelectElement
  | HTMLButtonElement
  | HTMLTextAreaElement;

export type ApiData = Array<JSONData>;
